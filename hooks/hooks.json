{
  "description": "Complete mediation: no built-in tool executes on the host. Points at a committed script, not a built binary, so a plugin whose binaries are missing denies rather than failing open.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/gate.sh",
            "args": [],
            "timeout": 10,
            "statusMessage": "nucleus: checking mediation"
          }
        ]
      }
    ]
  }
}
