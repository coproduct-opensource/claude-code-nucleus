{
  "description": "Complete mediation: no built-in tool executes on the host. Points at a committed script, not a built binary, so a plugin whose binaries are missing denies rather than failing open. PostToolUse carries an advisory host-free-space event: the failure it watches for kills the builder VM and presents as slow CI, never as a full disk.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/gate.sh",
            "args": [],
            "timeout": 10,
            "statusMessage": "nucleus: checking mediation"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/disk-warn.sh",
            "args": [],
            "timeout": 25,
            "statusMessage": "nucleus: checking host free space"
          }
        ]
      }
    ]
  }
}
