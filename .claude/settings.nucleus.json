{
  "$comment": [
    "The gate, as an OPT-IN settings file rather than as .claude/settings.json.",
    "",
    "Two reasons it is not the latter. The gate loads live, in the session that",
    "writes it, while an MCP server is read once at session start -- so writing",
    "the gate into the active settings file gives that session the denials",
    "without the mediated path they redirect to, including the Write needed to",
    "undo it. And this repo is built with cargo and committed with git, both of",
    "which are host Bash: under its own gate, the checkout cannot develop itself.",
    "",
    "Turn it on for a session that should be mediated:",
    "",
    "    claude --settings .claude/settings.nucleus.json \\",
    "           --disallowedTools Bash,Read,Write,Edit,Glob,Grep,WebFetch,WebSearch,NotebookEdit,Agent,Task",
    "",
    "Register the MCP server and restart FIRST -- see the README's Install order.",
    "If a session is ever stranded with no working tool, `!` at the prompt runs a",
    "command outside the hooks: `! git checkout .claude/settings.json`.",
    "",
    "The statusLine shows where the session sits in the lattice -- the pod, the",
    "allow/refuse counts, whether egress has tainted it, and the last refusal with",
    "its reason. refreshInterval is set because the event-driven triggers fire on",
    "each assistant message, not on each tool call, so a long turn would otherwise",
    "show a stale bar while the calls it describes are still happening."
  ],
  "statusLine": {
    "type": "command",
    "command": "ccn-mcp --statusline",
    "refreshInterval": 3
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [{ "type": "command", "command": "ccn-gate", "timeout": 10 }]
      }
    ]
  }
}
