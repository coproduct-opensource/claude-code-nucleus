[package]
name = "ccn-gate"
description = "PreToolUse hook: denies every unmediated built-in tool and names its pod-backed replacement"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "ccn-gate"
path = "src/main.rs"

[dependencies]
ccn-core = { path = "../ccn-core" }
serde = { workspace = true }
serde_json = { workspace = true }
